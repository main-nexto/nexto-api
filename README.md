# Nexto Courses API

## Overview

Nexto Courses is a RESTful API built with NodeJS + Express + Swagger for Nexto aplication.

## Prerequisites
- Node.js 10+
- NPM

## How to run

To run the server:

```
npm start
```

To view the Swagger UI:

```
open http://localhost:8080/docs
```
